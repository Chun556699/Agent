{
  "name": "agentdesk",
  "version": "0.1.0",
  "private": true,
  "description": "AgentDesk — a local-first agent work platform: multi-provider model gateway, tool/plugin system, sub-agents, durable threads.",
  "type": "module",
  "main": "desktop/main.mjs",
  "scripts": {
    "dev": "node server/index.js",
    "build": "npm --prefix web run build",
    "start": "NODE_ENV=production node server/index.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=22"
  }
}
